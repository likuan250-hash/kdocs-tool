# SteamGridDB 兜底封面源（可选）
# 注册 https://www.steamgriddb.com/ → 用 Steam 账号登录 → 右上角头像 → Profile → Preferences → API 标签 → Generate
# 直达地址：https://www.steamgriddb.com/profile/preferences/api
# 将生成的 key 填入下方，复制本文件为 .env 后填写（.env 已被 .gitignore 忽略，不会上传）
# 不填则 kdocs-tool 仅在 bl 无法提供封面、且非 Steam 游戏时跳过封面，不影响其他功能
STEAMGRIDDB_API_KEY=
